services:
  backend:
    build: ./backend
    container_name: lunchtool-backend
    restart: unless-stopped
    volumes:
      - ./data:/data
    environment:
      - DATABASE_URL=sqlite:////data/lunchtool.db
      - TZ=Europe/Amsterdam
      - GOOGLE_MAPS_API_KEY=${GOOGLE_MAPS_API_KEY:-}
      - WEERLIVE_KEY=${WEERLIVE_KEY:-}
      # DEV_EMAIL is only used when Cloudflare header is absent (local dev without CF tunnel)
      # - DEV_EMAIL=dev@example.com
    networks:
      - lunchtool

  frontend:
    build: ./frontend
    container_name: lunchtool
    restart: unless-stopped
    ports:
      - "3045:80"
    depends_on:
      - backend
    networks:
      - lunchtool

networks:
  lunchtool:
    name: lunchtool
